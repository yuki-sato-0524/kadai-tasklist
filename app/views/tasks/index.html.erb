<h1>登録タスク一覧</h1>
<ul>
  <% @tasks.each do |task|%>
  
    <li><%= link_to task.id, task %> タスク:<%= task.content %>>状況:<%= task.status %></li>
  
  <% end %>
</ul>

<%= link_to "タスクを登録する", new_task_path %> 
